#
# _config.yml for emoji.fileformat.info
#

exclude: [ "CNAME" ]
permalink: /blog/:year/:month/:day/:title/index.:output_ext
production_url: https://www.badger.sh
title: Badger
markdown: kramdown
plugins:
  - jekyll-redirect-from
defaults:
  - scope:
      path: ""
      type: "pages"
    values:
      layout: "default"
